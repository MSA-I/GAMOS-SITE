/**
 * directions-map.js — #routes "מסלולי הגעה" branded interactive map
 *
 * Owner : Directions Section (2026-06-10 redesign)
 * Spec  : CLAUDE.md §2 (Leaflet ~42KB self-hosted, this map ONLY),
 *         §4 (RTL-first), §8 (reduced motion), §9 (a11y),
 *         §10.3 (init/destroy, module-scoped, idempotent, no globals).
 *
 * Concept
 * -------
 * One Leaflet map (CARTO Positron tiles, recolored to brass/cream in
 * css/sections/directions.css) with a brass venue pin and THREE origin
 * tabs (מירושלים / מהמרכז / מצפון). Selecting a tab:
 *   1. flyToBounds([origin, venue]) so both fit the frame,
 *   2. swaps the active route polyline + animates it drawing on (GSAP
 *      stroke-dashoffset; falls back to instant if GSAP/ScrollTrigger
 *      or reduced-motion),
 *   3. updates the glass-card "≈NN דק׳ · NN ק״מ" label,
 *   4. re-points the three CTA hrefs (Waze / Google Maps / WhatsApp).
 *
 * Route geometry + durations/distances were pulled ONCE from the OSRM
 * demo router (real road-following lines) and baked in below — no runtime
 * routing dependency. Real navigation is delegated to the CTA apps.
 *
 * The map starts hidden under #contact (.reveal-pair), so we defer the
 * Leaflet init + invalidateSize() until the section first scrolls into
 * view (IntersectionObserver) — otherwise Leaflet reads a 0×0 container
 * and paints grey gaps.
 *
 * Public API: init(), destroy()
 */

// ---------------------------------------------------------------------------
// Constants — venue + per-origin route data (baked from OSRM, [lat,lng])
// ---------------------------------------------------------------------------

const VENUE = [31.7878076, 35.3371583]; // די זהב, פארק ישראל
const DEST_STR = "31.7878076,35.3371583";

// WhatsApp business number (matches #contact form data-wa-number).
const WA_NUMBER = "972779972343";

// Per-origin WhatsApp prompts (URL-encoded Hebrew) — "I'm coming from X,
// how do I get to Gamos?".
const WA_TEXT = {
  jerusalem:
    "%D7%A9%D7%9C%D7%95%D7%9D%2C%20%D7%90%D7%A0%D7%99%20%D7%9E%D7%92%D7%99%D7%A2%2F%D7%94%20%D7%9E%D7%99%D7%A8%D7%95%D7%A9%D7%9C%D7%99%D7%9D%20%D7%9C%D7%92%D7%90%D7%9E%D7%95%D7%A1%20%E2%80%94%20%D7%90%D7%99%D7%9A%20%D7%9B%D7%93%D7%90%D7%99%20%D7%9C%D7%94%D7%92%D7%99%D7%A2%3F",
  telaviv:
    "%D7%A9%D7%9C%D7%95%D7%9D%2C%20%D7%90%D7%A0%D7%99%20%D7%9E%D7%92%D7%99%D7%A2%2F%D7%94%20%D7%9E%D7%94%D7%9E%D7%A8%D7%9B%D7%96%20%D7%9C%D7%92%D7%90%D7%9E%D7%95%D7%A1%20%E2%80%94%20%D7%90%D7%99%D7%9A%20%D7%9B%D7%93%D7%90%D7%99%20%D7%9C%D7%94%D7%92%D7%99%D7%A2%3F",
  north:
    "%D7%A9%D7%9C%D7%95%D7%9D%2C%20%D7%90%D7%A0%D7%99%20%D7%9E%D7%92%D7%99%D7%A2%2F%D7%94%20%D7%9E%D7%A6%D7%A4%D7%95%D7%9F%20%D7%94%D7%90%D7%A8%D7%A5%20%D7%9C%D7%92%D7%90%D7%9E%D7%95%D7%A1%20%E2%80%94%20%D7%90%D7%99%D7%9A%20%D7%9B%D7%93%D7%90%D7%99%20%D7%9C%D7%94%D7%92%D7%99%D7%A2%3F",
  south:
    "%D7%A9%D7%9C%D7%95%D7%9D%2C%20%D7%90%D7%A0%D7%99%20%D7%9E%D7%92%D7%99%D7%A2%2F%D7%94%20%D7%9E%D7%93%D7%A8%D7%95%D7%9D%20%D7%94%D7%90%D7%A8%D7%A5%20%D7%9C%D7%92%D7%90%D7%9E%D7%95%D7%A1%20%E2%80%94%20%D7%90%D7%99%D7%9A%20%D7%9B%D7%93%D7%90%D7%99%20%D7%9C%D7%94%D7%92%D7%99%D7%A2%3F",
};

// Existing Waze venue deep-link (from the old #contact CTA) — navigates
// straight to the saved Gamos venue regardless of origin.
const WAZE_VENUE =
  "https://ul.waze.com/ul?preview_venue_id=23134526.231607403.655613&navigate=yes";

/**
 * ORIGINS — order matters: first entry is the default (checked) tab and,
 * in RTL, the rightmost. The card "from X" line comes from ORIGIN_LABELS (per
 * language); min/km are the OSRM-reported figures (rounded for display); coords
 * are the real road-following geometry as [lat,lng].
 */
const ORIGINS = {
  jerusalem: {
    min: 17,
    km: 16,
    coords: [[31.77903,35.22549],[31.78033,35.22774],[31.78139,35.22824],[31.78191,35.22808],[31.78304,35.22728],[31.78491,35.22754],[31.78881,35.22676],[31.78964,35.22682],[31.78983,35.22705],[31.79,35.22897],[31.79143,35.22877],[31.79285,35.22936],[31.79254,35.23106],[31.79286,35.23194],[31.79288,35.23349],[31.79306,35.23414],[31.79392,35.23481],[31.79511,35.23459],[31.79584,35.23546],[31.7958,35.23569],[31.79481,35.23628],[31.79408,35.23704],[31.79376,35.23785],[31.79352,35.2394],[31.79312,35.24007],[31.79257,35.24038],[31.79105,35.24065],[31.79035,35.24159],[31.79024,35.24328],[31.78962,35.24674],[31.78967,35.24914],[31.79071,35.2537],[31.79063,35.2549],[31.79032,35.25585],[31.78982,35.25664],[31.78916,35.25725],[31.7867,35.25836],[31.78572,35.25917],[31.7835,35.26285],[31.78302,35.26437],[31.78306,35.26615],[31.78346,35.26744],[31.78794,35.27485],[31.78839,35.27579],[31.78855,35.27669],[31.78838,35.27802],[31.78786,35.279],[31.78498,35.28109],[31.78443,35.28169],[31.784,35.28264],[31.78386,35.28414],[31.78411,35.29163],[31.78448,35.29276],[31.78522,35.29342],[31.78589,35.29362],[31.78785,35.29355],[31.78896,35.29422],[31.79761,35.30099],[31.79906,35.30289],[31.79983,35.30492],[31.80002,35.30626],[31.80001,35.30745],[31.79956,35.31081],[31.7998,35.31309],[31.80219,35.32147],[31.8031,35.32328],[31.80411,35.32475],[31.80405,35.32504],[31.79649,35.33154],[31.7945,35.33239],[31.79046,35.33583],[31.78964,35.3361],[31.78882,35.33607],[31.78888,35.33713],[31.78781,35.33716]],
  },
  telaviv: {
    min: 63,
    km: 72,
[31.80784,35.23656],[31.80813,35.24053],[31.80779,35.24177],[31.80713,35.24276],[31.8036,35.24481],[31.8029,35.24544],[31.80251,35.24607],[31.80219,35.24774],[31.80299,35.25367],[31.80267,35.25504],[31.80205,35.25596],[31.80078,35.25662],[31.78849,35.25969],[31.78488,35.25952],[31.78439,35.25987],[31.78411,35.26038],[31.78392,35.26194],[31.7832,35.26361],[31.78298,35.26471],[31.78306,35.26615],[31.78346,35.26744],[31.78794,35.27485],[31.78839,35.27579],[31.78855,35.27669],[31.78838,35.27802],[31.78786,35.279],[31.78498,35.28109],[31.78443,35.28169],[31.784,35.28264],[31.78386,35.28414],[31.78408,35.29121],[31.78424,35.29224],[31.78448,35.29276],[31.78522,35.29342],[31.78589,35.29362],[31.78785,35.29355],[31.78896,35.29422],[31.79761,35.30099],[31.79906,35.30289],[31.79983,35.30492],[31.80002,35.30626],[31.80001,35.30745],[31.79956,35.31081],[31.7998,35.31309],[31.80219,35.32147],[31.80411,35.32487],[31.79649,35.33154],[31.7945,35.33239],[31.79046,35.33583],[31.78964,35.3361],[31.78882,35.33607],[31.78888,35.33713],[31.78781,35.33716]],
  },
};

const ORIGIN_KEYS = ["jerusalem", "telaviv", "north", "south"];

/**
 * LANDMARKS — city/town labels shown along EACH route (per active tab), so
 * visitors read the distance scale by recognizing the places the drive
 * passes. Jerusalem's entry additionally lists neighborhood labels (shown
 * only on the Jerusalem tab). [lat, lng]. `anchor` nudges the label off the
 * point: "right" | "left" | "top" | "bottom".
 *
 * Always-on anchors (venue + the origin city) are handled separately; these
 * are the en-route waypoints that swap with the selected origin.
 */
// Candidate place pools per route. These are FILTERED at render time to keep
// only the ones the route polyline actually passes (within ON_ROUTE_MAX_M),
// and each surviving label is snapped onto the nearest point of the route line
// — so what shows is exactly "the cities/neighborhoods on the route". Generous
// pools are fine: anything off-route is dropped automatically.
const ROUTE_LABELS = {
  // Jerusalem tab carries NEIGHBORHOOD names (the venue sits just east of the
  // city, so the eastern-corridor neighborhoods are what the drive passes).
  jerusalem: [
    { name: "מרכז העיר",      at: [31.78197, 35.21957], anchor: "left" },
    { name: "העיר העתיקה",    at: [31.77829, 35.23195], anchor: "bottom" },
    { name: "הר הצופים",      at: [31.79226, 35.24617], anchor: "top" },
    { name: "הגבעה הצרפתית",  at: [31.80402, 35.23828], anchor: "top" },
    { name: "רמת אשכול",      at: [31.80179, 35.22288], anchor: "left" },
    { name: "פסגת זאב",       at: [31.82234, 35.24103], anchor: "left" },
    { name: "נווה יעקב",      at: [31.84221, 35.24206], anchor: "left" },
    { name: "מעלה אדומים",    at: [31.77300, 35.30000], anchor: "bottom" },
  ],
  // Center, via the Route 443 / Modiin corridor (the baked geometry).
  telaviv: [
    { name: "אור יהודה",  at: [32.02700, 34.86299], anchor: "left" },
    { name: "יהוד",       at: [32.03318, 34.89075], anchor: "top" },
    { name: "פתח תקווה",  at: [32.08776, 34.88600], anchor: "top" },
    { name: "ראש העין",   at: [32.09529, 34.95332], anchor: "top" },
    { name: "אלעד",       at: [32.05007, 34.95215], anchor: "left" },
    { name: "בן שמן",     at: [31.95228, 34.92194], anchor: "bottom" },
    { name: "שוהם",       at: [32.00048, 34.94654], anchor: "top" },
    { name: "מודיעין",    at: [31.90857, 35.00693], anchor: "top" },
    { name: "מכבים רעות", at: [31.89209, 35.03399], anchor: "bottom" },
    { name: "ירושלים",    at: [31.78197, 35.21957], anchor: "bottom" },
    { name: "מעלה אדומים",at: [31.77300, 35.30000], anchor: "bottom" },
  ],
  // North down the inland Cross-Israel corridor (Route 6) then Route 1.
  north: [
    { name: "יקנעם",         at: [32.64806, 35.09435], anchor: "left" },
    { name: "פרדס חנה",      at: [32.47500, 34.97514], anchor: "left" },
    { name: "באקה אל-גרבייה",at: [32.41971, 35.04283], anchor: "right" },
    { name: "קלנסווה",       at: [32.28486, 34.98017], anchor: "left" },
    { name: "טייבה",         at: [32.26712, 35.00890], anchor: "right" },
    { name: "כפר קאסם",      at: [32.11520, 34.97526], anchor: "left" },
    { name: "ראש העין",      at: [32.09529, 34.95332], anchor: "top" },
    { name: "שוהם",          at: [32.00048, 34.94654], anchor: "top" },
    { name: "מודיעין",       at: [31.90857, 35.00693], anchor: "top" },
    { name: "שער הגיא",      at: [31.81700, 35.02400], anchor: "bottom" },
    { name: "אבו גוש",       at: [31.80635, 35.10887], anchor: "top" },
    { name: "מבשרת ציון",    at: [31.80572, 35.15273], anchor: "bottom" },
    { name: "ירושלים",       at: [31.78197, 35.21957], anchor: "bottom" },
    { name: "מעלה אדומים",   at: [31.77300, 35.30000], anchor: "bottom" },
  ],
  // South via Route 6 / 40.
  south: [
    { name: "קרית גת",    at: [31.60940, 34.77120], anchor: "left" },
    { name: "קרית מלאכי", at: [31.73160, 34.74461], anchor: "left" },
    { name: "בית שמש",    at: [31.74621, 34.98868], anchor: "bottom" },
    { name: "שער הגיא",   at: [31.81700, 35.02400], anchor: "top" },
    { name: "אבו גוש",    at: [31.80635, 35.10887], anchor: "top" },
    { name: "מבשרת ציון", at: [31.80572, 35.15273], anchor: "top" },
    { name: "מוצא",       at: [31.79279, 35.16851], anchor: "bottom" },
    { name: "ירושלים",    at: [31.78197, 35.21957], anchor: "bottom" },
    { name: "מעלה אדומים",at: [31.77300, 35.30000], anchor: "bottom" },
  ],
};

// A label survives only if it lies within this many meters of the route line.
// Tightened (was 4200) so ONLY towns genuinely adjacent to the drawn route
// show — anything farther reads as "not on the route" and is dropped.
const ON_ROUTE_MAX_M = 2600;

// How far to nudge each label from its true location toward the nearest route
// point: 0 = stay at the real city, 1 = sit exactly on the line. ~0.6 keeps
// the label visibly close to BOTH the route and the city's real position.
const LABEL_BLEND = 0.6;

// Per-origin "start" city label (sits at the route's far end / origin city).
const ORIGIN_CITY = {
  jerusalem: "ירושלים",
  telaviv:   "תל אביב",
  north:     "חיפה",
  south:     "באר שבע",
};

// Extra national cities (the per-route pools above cover the four main
// corridors; these broaden coverage so a FREE-TEXT typed address anywhere in
// the country still finds ≥5 nearby towns along its route). [lat, lng].
const EXTRA_CITIES = [
  { name: "אשדוד",        at: [31.80400, 34.65530] },
  { name: "אשקלון",       at: [31.66880, 34.57430] },
  { name: "נתניה",        at: [32.32150, 34.85320] },
  { name: "כפר סבא",      at: [32.17500, 34.90690] },
  { name: "רעננה",        at: [32.18480, 34.87130] },
  { name: "הרצליה",       at: [32.16240, 34.84430] },
  { name: "ראשון לציון",  at: [31.97300, 34.80660] },
  { name: "רחובות",       at: [31.89280, 34.81130] },
  { name: "רמלה",         at: [31.92880, 34.86670] },
  { name: "לוד",          at: [31.95140, 34.89530] },
  { name: "אריאל",        at: [32.10560, 35.18940] },
  { name: "אילת",         at: [29.55770, 34.95190] },
  { name: "קרית שמונה",   at: [33.20750, 35.57050] },
  { name: "טבריה",        at: [32.79220, 35.53120] },
];

// National label pool for FREE-TEXT routes — union of every per-route candidate
// town, the four origin cities, and EXTRA_CITIES, deduped by name. Built once at
// module load (renderCustomLabels filters it to the towns near the typed route).
const CITY_POOL = (() => {
  const seen = new Set();
  const pool = [];
  const add = (name, at) => { if (name && !seen.has(name)) { seen.add(name); pool.push({ name, at }); } };
  for (const key of ORIGIN_KEYS) {
    for (const p of (ROUTE_LABELS[key] || [])) add(p.name, p.at);
    const o = ORIGINS[key];
    if (o) add(ORIGIN_CITY[key], o.coords[0]);
  }
  for (const c of EXTRA_CITIES) add(c.name, c.at);
  return pool;
})();

// Basemap tiles: warm beige land + naturally BLUE water, no built-in labels
// (our Hebrew labels stay clean). A gentle CSS tint warms it to brand without
// killing the blue sea (css/sections/directions.css) — the recolor filter is
// provider-agnostic, so MapTiler "basic" is a drop-in for CARTO Voyager.
//
// ⚠️ PRODUCTION (Constitution §14 / DEPLOYMENT-COSTS.md): the keyless CARTO
// endpoint below is for LOCAL DEV ONLY — it violates CARTO's ToS in production.
// Before go-live, create a free MapTiler key (cloud.maptiler.com), DOMAIN-LOCK
// it to gamos.co.il, and paste it into MAPTILER_KEY. With a key set, the map
// auto-switches to MapTiler (100k tiles/mo free); empty = CARTO dev fallback.
const MAPTILER_KEY = ""; // ← paste domain-restricted MapTiler key here for prod
const TILE_URL = MAPTILER_KEY
  ? `https://api.maptiler.com/maps/basic-v2/{z}/{x}/{y}.png?key=${MAPTILER_KEY}`
  : "https://{s}.basemaps.cartocdn.com/rastertiles/voyager_nolabels/{z}/{x}/{y}{r}.png";
const TILE_ATTR = MAPTILER_KEY
  ? '© <a href="https://www.maptiler.com/copyright/">MapTiler</a> © <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a>'
  : "© OpenStreetMap, © CARTO";

// Brass venue pin as an inline-SVG divIcon (no dependency on Leaflet's
// default marker PNGs). Pulse handled in CSS via .directions__pin.
const PIN_SVG =
  '<span class="directions__pin" aria-hidden="true">' +
  '<svg viewBox="0 0 32 44" width="32" height="44">' +
  '<path d="M16 0C7.7 0 1 6.7 1 15c0 10.5 13.2 27 14.0 28a1.3 1.3 0 0 0 2 0C17.8 42 31 25.5 31 15 31 6.7 24.3 0 16 0z" ' +
  'fill="var(--brass-deep, #8B6F46)" stroke="var(--ivory, #F5EFE6)" stroke-width="1.5"/>' +
  '<circle cx="16" cy="15" r="5.5" fill="var(--ivory, #F5EFE6)"/>' +
  "</svg></span>";

// ---------------------------------------------------------------------------
// i18n — fixed-string localisation (he / en / fr)
// ---------------------------------------------------------------------------
//
// This module injects its labels/ETA/status at RUNTIME (after Leaflet builds,
// after a tab click, after a geocode) — long past i18n.js's one-time DOM pass —
// so those strings can't carry data-i18n keys. Instead we localise them here
// against document.documentElement.lang and re-render on gamos:langchange (the
// same signal hero-scene.js listens to). Free-text addresses the user types are
// NEVER translated (they fall through PLACE_I18N unchanged).

/** Current UI language from <html lang>, clamped to a supported code. */
function lang() {
  const l = (document.documentElement.lang || "he").slice(0, 2).toLowerCase();
  return l === "en" || l === "fr" ? l : "he";
}

// ETA units + approx sign — "≈ 17 min · 16 km".
const APPROX = "≈";
const UNITS = {
  he: { min: "דק׳", km: "ק״מ" },
  en: { min: "min", km: "km" },
  fr: { min: "min", km: "km" },
};

// Origin tab / card "from X" labels, per language.
const ORIGIN_LABELS = {
  he: { jerusalem: "מירושלים",        telaviv: "מהמרכז",          north: "מהצפון",          south: "מהדרום" },
  en: { jerusalem: "From Jerusalem",  telaviv: "From the Center", north: "From the North",  south: "From the South" },
  fr: { jerusalem: "Depuis Jérusalem", telaviv: "Depuis le Centre", north: "Depuis le Nord", south: "Depuis le Sud" },
};

// Search-status messages, per language.
const STATUS = {
  he: {
    loading:   "המפה עדיין נטענת, נסו שוב בעוד רגע",
    calc:      "מחשב…",
    noAddress: "לא נמצאה כתובת, נסו שוב",
    noRoute:   "לא הצלחנו לחשב מסלול, נסו שוב",
  },
  en: {
    loading:   "The map is still loading, try again in a moment",
    calc:      "Calculating…",
    noAddress: "Address not found, try again",
    noRoute:   "Couldn't calculate a route, try again",
  },
  fr: {
    loading:   "La carte charge encore, réessayez dans un instant",
    calc:      "Calcul en cours…",
    noAddress: "Adresse introuvable, réessayez",
    noRoute:   "Impossible de calculer un itinéraire, réessayez",
  },
};

// City / neighborhood / venue name translations, keyed by the Hebrew name used
// in ORIGINS / ROUTE_LABELS / ORIGIN_CITY / EXTRA_CITIES. Any name not listed
// (e.g. a typed free-text address) falls back to the original string unchanged.
const PLACE_I18N = {
  "גאמוס":         { en: "GAMOS",              fr: "GAMOS" },
  // Origin cities
  "ירושלים":       { en: "Jerusalem",          fr: "Jérusalem" },
  "תל אביב":       { en: "Tel Aviv",           fr: "Tel Aviv" },
  "חיפה":          { en: "Haifa",              fr: "Haïfa" },
  "באר שבע":       { en: "Beersheba",          fr: "Beer-Sheva" },
  // Jerusalem neighborhoods
  "מרכז העיר":     { en: "City Center",        fr: "Centre-ville" },
  "העיר העתיקה":   { en: "Old City",           fr: "Vieille ville" },
  "הר הצופים":     { en: "Mount Scopus",       fr: "Mont Scopus" },
  "הגבעה הצרפתית": { en: "French Hill",         fr: "Colline française" },
  "רמת אשכול":     { en: "Ramat Eshkol",       fr: "Ramat Eshkol" },
  "פסגת זאב":      { en: "Pisgat Ze'ev",       fr: "Pisgat Ze'ev" },
  "נווה יעקב":     { en: "Neve Yaakov",        fr: "Neve Yaakov" },
  "מעלה אדומים":   { en: "Ma'ale Adumim",      fr: "Maale Adoumim" },
  // Towns along the routes
  "אור יהודה":     { en: "Or Yehuda",          fr: "Or Yehouda" },
  "יהוד":          { en: "Yehud",              fr: "Yehoud" },
  "פתח תקווה":     { en: "Petah Tikva",        fr: "Petah Tikva" },
  "ראש העין":      { en: "Rosh HaAyin",        fr: "Rosh HaAyin" },
  "אלעד":          { en: "Elad",               fr: "Elad" },
  "בן שמן":        { en: "Ben Shemen",         fr: "Ben Shemen" },
  "שוהם":          { en: "Shoham",             fr: "Shoham" },
  "מודיעין":       { en: "Modiin",             fr: "Modiin" },
  "מכבים רעות":    { en: "Maccabim-Reut",      fr: "Maccabim-Reout" },
  "יקנעם":         { en: "Yokneam",            fr: "Yokneam" },
  "פרדס חנה":      { en: "Pardes Hanna",       fr: "Pardes Hanna" },
  "באקה אל-גרבייה": { en: "Baqa al-Gharbiyye", fr: "Baqa al-Gharbiyye" },
  "קלנסווה":       { en: "Qalansawe",          fr: "Qalansawe" },
  "טייבה":         { en: "Tayibe",             fr: "Tayibe" },
  "כפר קאסם":      { en: "Kafr Qasim",         fr: "Kafr Qasim" },
  "שער הגיא":      { en: "Sha'ar HaGai",       fr: "Sha'ar HaGai" },
  "אבו גוש":       { en: "Abu Ghosh",          fr: "Abou Gosh" },
  "מבשרת ציון":    { en: "Mevaseret Zion",     fr: "Mevaseret Zion" },
  "קרית גת":       { en: "Kiryat Gat",         fr: "Kiryat Gat" },
  "קרית מלאכי":    { en: "Kiryat Malakhi",     fr: "Kiryat Malakhi" },
  "בית שמש":       { en: "Beit Shemesh",       fr: "Beit Shemesh" },
  "מוצא":          { en: "Motza",              fr: "Motza" },
  // Extra national cities (free-text coverage)
  "אשדוד":         { en: "Ashdod",             fr: "Ashdod" },
  "אשקלון":        { en: "Ashkelon",           fr: "Ashkelon" },
  "נתניה":         { en: "Netanya",            fr: "Netanya" },
  "כפר סבא":       { en: "Kfar Saba",          fr: "Kfar Saba" },
  "רעננה":         { en: "Ra'anana",           fr: "Raanana" },
  "הרצליה":        { en: "Herzliya",           fr: "Herzliya" },
  "ראשון לציון":   { en: "Rishon LeZion",      fr: "Rishon LeZion" },
  "רחובות":        { en: "Rehovot",            fr: "Rehovot" },
  "רמלה":          { en: "Ramla",              fr: "Ramla" },
  "לוד":           { en: "Lod",                fr: "Lod" },
  "אריאל":         { en: "Ariel",              fr: "Ariel" },
  "אילת":          { en: "Eilat",              fr: "Eilat" },
  "קרית שמונה":    { en: "Kiryat Shmona",      fr: "Kiryat Shmona" },
  "טבריה":         { en: "Tiberias",           fr: "Tibériade" },
};

/** Localise a place/city name to the given lang (fallback: the Hebrew name). */
function placeName(he, l = lang()) {
  if (l === "he") return he;
  const t = PLACE_I18N[he];
  return (t && t[l]) || he;
}

/** Format the ETA stat in the given lang — "≈ 17 min · 16 km". */
function formatEta(min, km, l = lang()) {
  const u = UNITS[l] || UNITS.he;
  return `${APPROX} ${min} ${u.min} · ${km} ${u.km}`;
}

// ---------------------------------------------------------------------------
// Module state (closure — no globals)
// ---------------------------------------------------------------------------

const state = {
  initialised: false,
  reducedMotion: false,
  root: null,            // [data-directions]
  mapEl: null,           // #directions-map
  map: null,             // L.Map
  routeLayer: null,      // active L.Polyline
  labelLayer: null,      // L.LayerGroup of per-route place labels
  io: null,              // IntersectionObserver (first-reveal gate)
  resizeObs: null,       // ResizeObserver (invalidateSize on resize)
  mqlMotion: null,       // matchMedia reduced-motion
  inputs: [],            // tab radio inputs
  onInput: null,         // change handler ref (for removal)
  searchForm: null,      // [data-directions-search]
  onSubmit: null,        // submit handler ref (for removal)
  searchMarker: null,    // custom address start label (L.marker)
  venueMarker: null,     // brass venue pin + "גאמוס" label (re-labelled on langchange)
  msgKey: "",            // active search-status KEY (STATUS.*) or "" — re-rendered on langchange
  customRoute: null,     // last free-text route { coords, startName, min, km } (for re-render)
  langHandler: null,     // gamos:langchange listener (added in init, removed in destroy)
  active: "jerusalem",
  built: false,          // Leaflet map constructed yet?
  leafletPromise: null,  // shared promise keeps concurrent callers idempotent
  leafletAttempt: 0,     // invalidates an in-flight promise during destroy()
  leafletCbs: [],        // callbacks queued until BOTH CSS and window.L are ready
};

// ---------------------------------------------------------------------------
// Helpers
// ---------------------------------------------------------------------------

function gsapReady() {
  return typeof window.gsap !== "undefined" && state.reducedMotion === false;
}

function setCardAndCtas(key) {
  const o = ORIGINS[key];
  if (!o) return;

  const l = lang();
  const etaOrigin = state.root.querySelector("[data-directions-eta-origin]");
  const etaStat = state.root.querySelector("[data-directions-eta]");
  if (etaOrigin) etaOrigin.textContent = (ORIGIN_LABELS[l] || ORIGIN_LABELS.he)[key];
  if (etaStat) etaStat.textContent = formatEta(o.min, o.km, l);

  // Google Maps directions: origin = first route point, destination = venue.
  const origin = o.coords[0];
  const gEl = state.root.querySelector("[data-directions-google]");
  if (gEl) {
    gEl.href =
      "https://www.google.com/maps/dir/?api=1" +
      `&origin=${origin[0]},${origin[1]}` +
      `&destination=${DEST_STR}&travelmode=driving`;
  }
  // Waze → saved venue (origin-independent; Waze uses the user's GPS).
  const wzEl = state.root.querySelector("[data-directions-waze]");
  if (wzEl) wzEl.href = WAZE_VENUE;
  // WhatsApp → origin-specific question.
  const waEl = state.root.querySelector("[data-directions-wa]");
  if (waEl) waEl.href = `https://wa.me/${WA_NUMBER}?text=${WA_TEXT[key]}`;
}

function syncTabAria() {
  for (const input of state.inputs) {
    const on = input.value === state.active;
    input.setAttribute("aria-selected", on ? "true" : "false");
    input.closest(".directions__tab")?.classList.toggle("is-active", on);
  }
}

/** Build a non-interactive text label divIcon for a place. */
function placeLabel(L, place, variant) {
  const anchor = place.anchor || "right";
  // Localise the label to the current lang (typed free-text names fall through
  // PLACE_I18N unchanged). Read lang() at render time so a langchange re-render
  // picks up the new language.
  const name = placeName(place.name);
  return L.marker(place.at, {
    icon: L.divIcon({
      className: "directions__label-wrap",
      html:
        `<span class="directions__place directions__place--${anchor}` +
        (variant ? ` directions__place--${variant}` : "") +
        '" aria-hidden="true">' +
        '<b class="directions__place-name">' + name + "</b>" +
        "</span>",
      iconSize: [0, 0],
      iconAnchor: [0, 0],
    }),
    keyboard: false,
    interactive: false,
  });
}

/**
 * Closest point on a route polyline to `pt`, in a local equirectangular
 * projection (meters). Returns { distM, at:[lat,lng] } — the snapped point on
 * the line. Used to keep only labels that actually sit ON the route and to
 * place them exactly on it.
 */
function nearestOnRoute(pt, coords) {
  const latRef = (pt[0] * Math.PI) / 180;
  const mLat = 111320;
  const mLon = 111320 * Math.cos(latRef);
  const X = (p) => [p[1] * mLon, p[0] * mLat];     // [lng,lat] → [x,y] meters
  const toLL = (x, y) => [y / mLat, x / mLon];
  const [px, py] = X(pt);

  // `order` (segment index + t) lets callers sort labels ALONG the route.
  let best = { distM: Infinity, at: pt, order: 0 };
  for (let i = 0; i < coords.length - 1; i++) {
    const [ax, ay] = X(coords[i]);
    const [bx, by] = X(coords[i + 1]);
    const dx = bx - ax, dy = by - ay;
    const len2 = dx * dx + dy * dy;
    let t = len2 ? ((px - ax) * dx + (py - ay) * dy) / len2 : 0;
    t = Math.max(0, Math.min(1, t));
    const cx = ax + t * dx, cy = ay + t * dy;
    const d = Math.hypot(px - cx, py - cy);
    if (d < best.distM) best = { distM: d, at: toLL(cx, cy), order: i + t };
  }
  return best;
}

/** Rebuild the place labels for the active origin — only those ON the route. */
function renderRouteLabels(key) {
  const L = window.L;
  if (!L || !state.map) return;

  if (state.labelLayer) { state.labelLayer.remove(); state.labelLayer = null; }
  const group = L.layerGroup();
  const o = ORIGINS[key];
  if (!o) return;

  // Declutter (2026-06-22, user bug: "the cities are all grouped in the same
  // place"): many candidate towns on a route sit close together (esp. the
  // north/south routes), so without a spacing guard their labels pile into one
  // unreadable cluster. Drop any new label that sits within MIN_GAP_M (meters)
  // of one already shown (or the destination pin). Greedy in data order — the
  // pools are ordered along the route, so the survivors stay spread out, each
  // reading next to its own town. Done in METERS (not screen px) because this
  // runs BEFORE drawRoute()/fitBounds() set the map view, so pixel projection
  // isn't available; the threshold scales with the route's span so the on-screen
  // spacing stays roughly constant whatever zoom fitBounds picks.
  const M_PER_DEG = 111320;
  const distM = (a, b) => {
    const dx = (a[1] - b[1]) * M_PER_DEG * Math.cos((a[0] * Math.PI) / 180);
    const dy = (a[0] - b[0]) * M_PER_DEG;
    return Math.hypot(dx, dy);
  };
  let minLat = Infinity, maxLat = -Infinity, minLng = Infinity, maxLng = -Infinity;
  for (const cc of o.coords) {
    minLat = Math.min(minLat, cc[0]); maxLat = Math.max(maxLat, cc[0]);
    minLng = Math.min(minLng, cc[1]); maxLng = Math.max(maxLng, cc[1]);
  }
  const spanM = distM([minLat, minLng], [maxLat, maxLng]);
  const MIN_GAP_M = spanM / 10;       // ~10 label-slots across the route diagonal
  const placed = [];
  const tooClose = (at) => placed.some((q) => distM(at, q) < MIN_GAP_M);
  // Seed with the destination pin so no town label collides with גאמוס.
  placed.push(o.coords[o.coords.length - 1]);

  // Origin city label sits at the route's start point (always on-route).
  if (ORIGIN_CITY[key]) {
    placeLabel(L, { name: ORIGIN_CITY[key], at: o.coords[0], anchor: "top" }, "origin").addTo(group);
    placed.push(o.coords[0]);
  }

  // Keep only candidates within ON_ROUTE_MAX_M of the line. Place each label
  // BETWEEN its true location and the nearest route point (LABEL_BLEND toward
  // the line) so it reads as close to both the route AND the real city — not
  // detached from either. Skip any that would overlap an already-placed label.
  for (const p of (ROUTE_LABELS[key] || [])) {
    const near = nearestOnRoute(p.at, o.coords);
    if (near.distM > ON_ROUTE_MAX_M) continue;     // off-route → drop
    const at = [
      p.at[0] + (near.at[0] - p.at[0]) * LABEL_BLEND,
      p.at[1] + (near.at[1] - p.at[1]) * LABEL_BLEND,
    ];
    if (tooClose(at)) continue;                     // would overlap → drop
    placeLabel(L, { name: p.name, at, anchor: p.anchor }).addTo(group);
    placed.push(at);
  }

  group.addTo(state.map);
  state.labelLayer = group;
}

/**
 * Labels for a FREE-TEXT (geocoded) route: the typed start point + at least 5
 * towns from CITY_POOL that lie nearest the typed route line. Mirrors
 * renderRouteLabels' snapping/blend/declutter, but the candidate pool is the
 * national CITY_POOL (the baked ROUTE_LABELS are keyed to the preset origins).
 */
function renderCustomLabels(routeCoords, startName) {
  const L = window.L;
  if (!L || !state.map || !routeCoords || routeCoords.length < 2) return;

  if (state.labelLayer) { state.labelLayer.remove(); state.labelLayer = null; }
  const group = L.layerGroup();

  const M_PER_DEG = 111320;
  const distM = (a, b) => {
    const dx = (a[1] - b[1]) * M_PER_DEG * Math.cos((a[0] * Math.PI) / 180);
    const dy = (a[0] - b[0]) * M_PER_DEG;
    return Math.hypot(dx, dy);
  };
  let minLat = Infinity, maxLat = -Infinity, minLng = Infinity, maxLng = -Infinity;
  for (const c of routeCoords) {
    minLat = Math.min(minLat, c[0]); maxLat = Math.max(maxLat, c[0]);
    minLng = Math.min(minLng, c[1]); maxLng = Math.max(maxLng, c[1]);
  }
  const MIN_GAP_M = distM([minLat, minLng], [maxLat, maxLng]) / 10;

  // Seed declutter with the venue (route end) so no town label hits גאמוס.
  const placed = [routeCoords[routeCoords.length - 1]];

  // Start label sits ON the route's first point (so the typed address name is
  // anchored to where the line begins — never detached from the map).
  if (startName) {
    placeLabel(L, { name: startName, at: routeCoords[0], anchor: "top" }, "origin").addTo(group);
    placed.push(routeCoords[0]);
  }

  // Score every candidate town by distance to the route line; keep the nearest.
  const scored = CITY_POOL
    .map((c) => {
      const near = nearestOnRoute(c.at, routeCoords);
      return { name: c.name, at: c.at, distM: near.distM, snap: near.at, order: near.order };
    })
    .sort((a, b) => a.distM - b.distM);

  const NEAR_M = 6000;                       // "near the route" threshold
  let pool = scored.filter((s) => s.distM <= NEAR_M);
  if (pool.length < 5) pool = scored.slice(0, 5);  // guarantee ≥5 nearest
  pool.sort((a, b) => a.order - b.order);          // read in along-route order

  const blended = (s) => [
    s.at[0] + (s.snap[0] - s.at[0]) * LABEL_BLEND,
    s.at[1] + (s.snap[1] - s.at[1]) * LABEL_BLEND,
  ];

  let shown = 0;
  for (const s of pool) {
    const at = blended(s);
    if (placed.some((q) => distM(at, q) < MIN_GAP_M)) continue;  // declutter
    placeLabel(L, { name: s.name, at }).addTo(group);
    placed.push(at); shown++;
  }
  // If declutter dropped us under 5, relax the spacing and top up from nearest.
  if (shown < 5) {
    for (const s of scored) {
      if (shown >= 5) break;
      const at = blended(s);
      if (placed.some((q) => distM(at, q) < MIN_GAP_M * 0.5)) continue;
      placeLabel(L, { name: s.name, at }).addTo(group);
      placed.push(at); shown++;
    }
  }

  group.addTo(state.map);
  state.labelLayer = group;
}

/** Draw (or redraw) the polyline for the active origin and animate it on. */
function drawRoute(key, { animate } = { animate: true }) {
  const L = window.L;
  const o = ORIGINS[key];
  if (!L || !o || !state.map) return;

  if (state.routeLayer) {
    state.routeLayer.remove();
    state.routeLayer = null;
  }

  state.routeLayer = L.polyline(o.coords, {
    className: "directions__route",
    // Color/width/glow live in CSS (.directions__route) so the brand stays
    // single-sourced in tokens. These are inert fallbacks.
    color: "#8B6F46",
    weight: 4,
    opacity: 1,
    lineJoin: "round",
    lineCap: "round",
  }).addTo(state.map);

  // Animate the SVG path drawing on via stroke-dashoffset (GSAP if present).
  const path = state.routeLayer.getElement();
  if (!path) return;

  if (!animate || !gsapReady()) {
    path.style.strokeDasharray = "none";
    path.style.strokeDashoffset = "0";
    return;
  }
  const len = path.getTotalLength ? path.getTotalLength() : 0;
  if (!len) return;
  path.style.strokeDasharray = String(len);
  path.style.strokeDashoffset = String(len);
  window.gsap.to(path.style, {
    strokeDashoffset: 0,
    duration: 1.1,
    ease: "power2.out",
    onComplete() {
      // Release the dash so a later hover/style change doesn't re-clip it.
      path.style.strokeDasharray = "none";
    },
  });
}

function selectOrigin(key, { fly } = { fly: true }) {
  if (!ORIGINS[key]) return;
  // Leaving a custom address search → drop its start label + stored route.
  if (state.searchMarker) { state.searchMarker.remove(); state.searchMarker = null; }
  state.customRoute = null;
  state.active = key;
  syncTabAria();
  setCardAndCtas(key);
  renderRouteLabels(key);
  drawRoute(key, { animate: true });

  if (!state.map) return;
  const L = window.L;
  const bounds = L.latLngBounds(ORIGINS[key].coords).extend(VENUE);
  const opts = fitPadding();
  if (fly && !state.reducedMotion) {
    state.map.flyToBounds(bounds, { ...opts, duration: 0.9 });
  } else {
    state.map.fitBounds(bounds, opts);
  }
}

// ---------------------------------------------------------------------------
// Free-text address search → live geocode + driving route/ETA
// ---------------------------------------------------------------------------
//
// ponytail: geocoding (Photon) + routing (OSRM) run against keyless PUBLIC
// servers. NOTE: we use Photon (photon.komoot.io), NOT Nominatim — Nominatim
// returns HTTP 403 to browser/web-app requests (it forbids being used as a
// geocoding backend), which silently broke the whole search. Photon is built
// for browser/autocomplete use and sends CORS `*`. Both are fine for a
// low-traffic venue map but their usage policies forbid heavy production use —
// flagged in CLAUDE.md §14 / DEPLOYMENT-COSTS.md as a follow-up if traffic grows
// (swap to a keyed / self-hosted geocoder+router). No new dependency — plain
// fetch + the existing self-hosted Leaflet.

// Israel bounding box (minLon,minLat,maxLon,maxLat) — keeps generic street names
// (e.g. "הרצל 50") from matching the same street abroad.
const PHOTON_BBOX = "34.2,29.45,35.95,33.5";

/** Geocode a free-text address to [lat,lng] via Photon (IL-boxed). null if none. */
async function geocodeAddress(q) {
  try {
    const url =
      "https://photon.komoot.io/api/?limit=1&bbox=" + PHOTON_BBOX +
      "&q=" + encodeURIComponent(q);
    const res = await fetch(url);
    if (!res.ok) return null;
    const data = await res.json();
    const f = data && data.features && data.features[0];
    const c = f && f.geometry && f.geometry.coordinates; // [lng, lat]
    if (!c || !Number.isFinite(c[0]) || !Number.isFinite(c[1])) return null;
    return [c[1], c[0]];
  } catch {
    return null;
  }
}

/**
 * Driving route from `from` ([lat,lng]) to the venue via OSRM. Returns
 * { coords:[[lat,lng]…], min, km } (road-following geometry), or null.
 */
async function fetchRoute(from) {
  try {
    const url =
      "https://router.project-osrm.org/route/v1/driving/" +
      `${from[1]},${from[0]};${VENUE[1]},${VENUE[0]}` +
      "?overview=full&geometries=geojson";
    const res = await fetch(url);
    if (!res.ok) return null;
    const data = await res.json();
    const route = data && data.routes && data.routes[0];
    if (!route || !route.geometry || !route.geometry.coordinates) return null;
    return {
      coords: route.geometry.coordinates.map(([lng, lat]) => [lat, lng]),
      min: Math.round(route.duration / 60),
      km: Math.round(route.distance / 1000),
    };
  } catch {
    return null;
  }
}

/**
 * Show / hide the inline status message under the search input. Takes a STATUS
 * KEY (loading|calc|noAddress|noRoute) or "" to hide — stored so a langchange can
 * re-render the SAME message in the new language (applyLang → setSearchMsg).
 */
function setSearchMsg(key) {
  state.msgKey = key || "";
  const msg = state.root && state.root.querySelector("[data-directions-msg]");
  if (!msg) return;
  if (state.msgKey) {
    const s = STATUS[lang()] || STATUS.he;
    msg.textContent = s[state.msgKey] || "";
    msg.hidden = false;
  } else {
    msg.textContent = "";
    msg.hidden = true;
  }
}

/**
 * Handle a free-text address submit: geocode → route → draw + fit + update the
 * card. Mirrors drawRoute()'s polyline options so the custom route reads
 * identically to a baked one. Labels come from renderCustomLabels (national
 * CITY_POOL), since the baked ROUTE_LABELS are keyed to the preset origins.
 * Guards on Leaflet + a built map; otherwise no-ops.
 */
async function onSearchSubmit(e) {
  e.preventDefault();
  const L = window.L;
  const input = state.root && state.root.querySelector("[data-directions-address]");
  const btn = state.searchForm && state.searchForm.querySelector("button[type=submit]");
  if (!input) return;

  const q = input.value.trim();
  if (!q) { input.focus(); return; }

  // Map not ready (vendor failed / not yet built) → no-op gracefully.
  if (!L || !state.map) {
    setSearchMsg("loading");
    return;
  }

  if (btn) btn.disabled = true;
  setSearchMsg("calc");

  const coords = await geocodeAddress(q);
  if (!coords) {
    setSearchMsg("noAddress");
    if (btn) btn.disabled = false;
    return;
  }

  const route = await fetchRoute(coords);
  if (!route) {
    setSearchMsg("noRoute");
    if (btn) btn.disabled = false;
    return;
  }

  // Draw the custom route — mirror drawRoute()'s polyline options exactly.
  if (state.routeLayer) { state.routeLayer.remove(); state.routeLayer = null; }
  // Clear the baked place labels — they belong to a baked origin, not this one.
  if (state.labelLayer) { state.labelLayer.remove(); state.labelLayer = null; }
  if (state.searchMarker) { state.searchMarker.remove(); state.searchMarker = null; }
  state.routeLayer = L.polyline(route.coords, {
    className: "directions__route",
    color: "#8B6F46",
    weight: 4,
    opacity: 1,
    lineJoin: "round",
    lineCap: "round",
  }).addTo(state.map);
  state.map.fitBounds(state.routeLayer.getBounds(), fitPadding());

  // Labels: the typed start point (anchored to the route's first point so its
  // name never floats detached from the map) + ≥5 towns nearest the typed route.
  const startName = q.length > 28 ? q.slice(0, 28) + "…" : q;
  renderCustomLabels(route.coords, startName);

  // Remember the custom route so a later langchange can re-render its labels +
  // re-format its ETA (the town labels translate; the typed startName does not).
  state.customRoute = { coords: route.coords, startName, min: route.min, km: route.km };

  // Update the glass card (same fields setCardAndCtas touches).
  renderCustomCard(state.customRoute);
  const gEl = state.root.querySelector("[data-directions-google]");
  if (gEl) {
    gEl.href =
      "https://www.google.com/maps/dir/?api=1" +
      `&origin=${coords[0]},${coords[1]}` +
      `&destination=${DEST_STR}&travelmode=driving`;
  }
  // Waze / WhatsApp left as-is (venue-deep-link / generic question).

  // Custom route is now "active" — drop the baked tabs' visual selection.
  state.active = null;
  for (const tab of state.inputs) {
    tab.checked = false;
    tab.setAttribute("aria-selected", "false");
    tab.closest(".directions__tab")?.classList.remove("is-active");
  }

  setSearchMsg("");
  if (btn) btn.disabled = false;
}

/**
 * Reserve the floating glass card's footprint so route labels never hide
 * behind it. The card sits at the bottom inline-end corner — in RTL that's
 * the LEFT edge — so we inset the top-left padding by the card width. On
 * mobile the card is a static bar below the map (not overlaid), so we fall
 * back to symmetric padding under 768px.
 */
function fitPadding() {
  const base = 40;
  const overlaid = !window.matchMedia("(max-width: 768px)").matches;
  const card = state.root && state.root.querySelector(".directions__card");
  if (!overlaid || !card) return { padding: [base, base] };
  const w = Math.min(card.offsetWidth || 320, 420);
  // [x, y]: extra left padding = card width + gap; extra bottom for the card height.
  return {
    paddingTopLeft: [w + 28, base],
    paddingBottomRight: [base, base],
  };
}

/**
 * Update the glass card's "from X" line + ETA for a free-text route (the typed
 * startName is never translated; the ETA reformats per language). Shared by
 * onSearchSubmit and applyLang so both render the card identically.
 */
function renderCustomCard(route) {
  const etaOrigin = state.root.querySelector("[data-directions-eta-origin]");
  const etaStat = state.root.querySelector("[data-directions-eta]");
  if (etaOrigin) etaOrigin.textContent = route.startName;
  if (etaStat) etaStat.textContent = formatEta(route.min, route.km);
}

/** Re-label the persistent venue pin ("גאמוס") in the current language. */
function updateVenuePinLabel() {
  if (!state.venueMarker) return;
  const el = state.venueMarker.getElement();
  const b = el && el.querySelector(".directions__pin-name");
  if (b) b.textContent = placeName("גאמוס");
}

/**
 * Re-render every runtime-injected string in the current language. Fired by the
 * gamos:langchange listener (i18n.js can't reach these — they're built after its
 * one-time DOM pass). Re-runs the card + place labels + venue pin without any
 * fly/scroll, honouring whichever route is active (baked tab or free-text).
 */
function applyLang() {
  updateVenuePinLabel();
  if (state.active) {
    // Baked origin tab active → refresh card + relabel the route in-place.
    setCardAndCtas(state.active);
    renderRouteLabels(state.active);
  } else if (state.customRoute) {
    // Free-text route active → relabel towns (translate) + reformat the ETA.
    // The typed startName stays as-is (never translated).
    renderCustomLabels(state.customRoute.coords, state.customRoute.startName);
    renderCustomCard(state.customRoute);
  }
  // Re-render any visible search-status message (e.g. "Address not found") so it
  // follows the language toggle instead of staying stuck in the previous one.
  setSearchMsg(state.msgKey);
}

// ---------------------------------------------------------------------------
// Map construction (deferred until first reveal)
// ---------------------------------------------------------------------------

/**
 * Lazy-load the self-hosted Leaflet vendor (CSS + classic UMD script) the first
 * time the map is needed, then run `cb` once BOTH the stylesheet and window.L
 * exist. (2026-07-21 perf pass — the vendor was removed from index.html
 * <head>; #routes is the last
 * section, so ~62KB eager on every visit was waste.) Idempotent: concurrent
 * callers share one promise and queue on state.leafletCbs. A failed resource is
 * removed and the callbacks stay queued, so a later call can retry cleanly. The
 * tabs/CTAs remain functional while the map degrades gracefully.
 */
const LEAFLET_CSS_SELECTOR =
  'link[data-leaflet-css], link[rel="stylesheet"][href="/assets/vendor/leaflet.css"]';
const LEAFLET_JS_SELECTOR =
  'script[data-leaflet-js], script[src="/assets/vendor/leaflet.js"]';

function stylesheetReady(link) {
  if (!link) return false;
  if (link.dataset.leafletState === "loading") return false;
  if (link.dataset.leafletState === "ready") return true;
  try {
    return Boolean(link.sheet);
  } catch {
    // A loaded cross-origin stylesheet may deny sheet inspection. Leaflet is
    // self-hosted today, but treating that case as ready keeps this robust.
    return true;
  }
}

function loadLeafletCss() {
  let link = document.querySelector(LEAFLET_CSS_SELECTOR);
  if (stylesheetReady(link)) return Promise.resolve();

  // A prior failed or interrupted attempt can leave a dead node whose load/error
  // event has already fired. Replace it so the next attempt is genuinely new.
  if (link) link.remove();
  link = document.createElement("link");
  link.rel = "stylesheet";
  link.href = "/assets/vendor/leaflet.css";
  link.setAttribute("data-leaflet-css", "");
  link.dataset.leafletState = "loading";

  return new Promise((resolve, reject) => {
    link.addEventListener("load", () => {
      link.dataset.leafletState = "ready";
      resolve();
    }, { once: true });
    link.addEventListener("error", () => {
      link.remove();
      reject(new Error("Leaflet stylesheet failed to load"));
    }, { once: true });
    document.head.appendChild(link);
  });
}

function loadLeafletJs() {
  if (typeof window.L !== "undefined") return Promise.resolve();

  // Without window.L an existing node is either failed or from an interrupted
  // attempt. Replacing it prevents a retry from waiting on an event already lost.
  const existing = document.querySelector(LEAFLET_JS_SELECTOR);
  if (existing) existing.remove();

  const script = document.createElement("script");
  script.src = "/assets/vendor/leaflet.js";
  script.async = true;
  script.setAttribute("data-leaflet-js", "");

  return new Promise((resolve, reject) => {
    script.addEventListener("load", () => {
      if (typeof window.L !== "undefined") {
        resolve();
      } else {
        script.remove();
        reject(new Error("Leaflet script loaded without exposing window.L"));
      }
    }, { once: true });
    script.addEventListener("error", () => {
      script.remove();
      reject(new Error("Leaflet script failed to load"));
    }, { once: true });
    document.head.appendChild(script);
  });
}

function runLeafletCallbacks() {
  const cbs = state.leafletCbs.splice(0);
  for (const fn of cbs) fn();
}

function ensureLeaflet(cb) {
  if (typeof cb === "function" && !state.leafletCbs.includes(cb)) {
    state.leafletCbs.push(cb);
  }
  if (state.leafletPromise) return state.leafletPromise;

  const existingCss = document.querySelector(LEAFLET_CSS_SELECTOR);
  if (typeof window.L !== "undefined" && stylesheetReady(existingCss)) {
    runLeafletCallbacks();
    return Promise.resolve(true);
  }

  const attempt = ++state.leafletAttempt;
  const promise = Promise.all([loadLeafletCss(), loadLeafletJs()])
    .then(() => {
      if (attempt !== state.leafletAttempt) return false;
      runLeafletCallbacks();
      return true;
    })
    .catch(() => {
      // Resource helpers remove only the failed node. Keep callbacks queued and
      // reset the shared promise in finally, allowing the next call to retry.
      return false;
    })
    .finally(() => {
      if (attempt === state.leafletAttempt) {
        state.leafletPromise = null;
      }
    });

  state.leafletPromise = promise;
  return promise;
}

function buildMap() {
  if (state.built) return;
  const L = window.L;
  if (!L || !state.mapEl) return;

  state.map = L.map(state.mapEl, {
    zoomControl: true,
    scrollWheelZoom: true,    // wheel over the map zooms it; off the map the
                              // page scrolls normally (Leaflet only captures
                              // wheel events while the cursor is over the map).
    dragging: true,
    touchZoom: true,
    doubleClickZoom: true,
    boxZoom: false,
    keyboard: true,
    attributionControl: true,
  });

  L.tileLayer(TILE_URL, {
    attribution: TILE_ATTR,
    subdomains: "abcd",
    maxZoom: 19,
    detectRetina: true,
  }).addTo(state.map);

  // (Per-route place labels are rendered by renderRouteLabels() on each
  // origin selection — see selectOrigin().)

  // Venue pin + "גאמוס" label (localised; kept in state so langchange re-labels).
  state.venueMarker = L.marker(VENUE, {
    icon: L.divIcon({
      className: "directions__pin-wrap",
      html: PIN_SVG + '<b class="directions__pin-name" aria-hidden="true">' + placeName("גאמוס") + "</b>",
      iconSize: [32, 44],
      iconAnchor: [16, 44],
    }),
    keyboard: false,
    interactive: false,
  }).addTo(state.map);

  state.built = true;

  // First paint: fit the default origin (no fly on first reveal).
  selectOrigin(state.active, { fly: false });
  // Leaflet measured the container while it may still have been laid out
  // under #contact — nudge a size recalc on the next frame.
  requestAnimationFrame(() => state.map && state.map.invalidateSize());

  // Keep size correct on container resize (responsive stage height).
  if (typeof ResizeObserver === "function") {
    state.resizeObs = new ResizeObserver(() => {
      if (state.map) state.map.invalidateSize();
    });
    state.resizeObs.observe(state.mapEl);
  }
}

// ---------------------------------------------------------------------------
// Public API
// ---------------------------------------------------------------------------

export function init() {
  if (state.initialised) return;

  const root = document.querySelector("[data-directions]");
  const mapEl = document.getElementById("directions-map");
  if (!root || !mapEl) return;             // section not on this page

  state.initialised = true;
  state.root = root;
  state.mapEl = mapEl;

  state.mqlMotion = window.matchMedia("(prefers-reduced-motion: reduce)");
  state.reducedMotion = state.mqlMotion.matches;

  // Wire tabs (works even before the map builds — selectOrigin guards on map).
  state.inputs = Array.from(
    root.querySelectorAll('input[name="directions-origin"]')
  );
  const checked = state.inputs.find((i) => i.checked);
  state.active = checked ? checked.value : "jerusalem";

  state.onInput = (e) => {
    const val = e.target && e.target.value;
    if (val) selectOrigin(val, { fly: true });
  };
  for (const input of state.inputs) {
    input.addEventListener("change", state.onInput);
  }
  syncTabAria();
  setCardAndCtas(state.active);

  // Free-text address search (geocode + route/ETA). Lives alongside the tabs;
  // submitting restores onInput → selectOrigin for any tab clicked afterward.
  state.searchForm = root.querySelector("[data-directions-search]");
  if (state.searchForm) {
    state.onSubmit = onSearchSubmit;
    state.searchForm.addEventListener("submit", state.onSubmit);
  }

  // Re-localise runtime strings when i18n.js toggles the language. (setCardAndCtas
  // above already used lang() for the INITIAL render, so a page booted in en/fr
  // shows translated text without a toggle; deferred buildMap() likewise renders
  // its labels via placeName()/lang() at build time.)
  state.langHandler = () => applyLang();
  document.addEventListener("gamos:langchange", state.langHandler);

  // Leaflet vendor is NO LONGER in index.html <head> (2026-07-21 perf pass —
  // ~62KB was eager-loaded on every visit for a last-section map). Load it on
  // demand: when #routes approaches the viewport, inject the CSS + script, then
  // build the map once BOTH resources resolve (ensureLeaflet). This preserves
  // the original deferred-build intent — the section is hidden under #contact
  // (.reveal-pair)
  // at load, so an early 0×0 measure would paint grey gaps — while taking the
  // vendor off the critical path. If loading fails, ensureLeaflet degrades
  // gracefully (tabs still re-point CTAs, just no map). Never throws.
  const startMap = () => ensureLeaflet(buildMap);

  if (typeof IntersectionObserver === "function") {
    state.io = new IntersectionObserver(
      (entries) => {
        for (const entry of entries) {
          if (entry.isIntersecting) {
            const io = state.io;
            startMap().then((ready) => {
              // Disconnect only after success. On failure the observer remains
              // available, so leaving/re-entering the reveal zone can retry.
              if (ready && state.io === io) {
                state.io.disconnect();
                state.io = null;
              }
            });
            break;
          }
        }
      },
      // ~1000px ahead so the vendor + tiles finish loading before #routes is
      // actually on-screen (was 200px when the vendor was already in <head>).
      { rootMargin: "1000px 0px" }
    );
    state.io.observe(root);
  } else {
    // No IntersectionObserver → load + build immediately (still off the
    // critical path: init() runs from the deferred ESM module entry).
    startMap();
  }
}

export function destroy() {
  if (state.onInput) {
    for (const input of state.inputs) {
      input.removeEventListener("change", state.onInput);
    }
  }
  if (state.searchForm && state.onSubmit) {
    state.searchForm.removeEventListener("submit", state.onSubmit);
  }
  state.searchForm = null;
  state.onSubmit = null;
  if (state.langHandler) {
    document.removeEventListener("gamos:langchange", state.langHandler);
    state.langHandler = null;
  }
  if (state.io) { state.io.disconnect(); state.io = null; }
  if (state.resizeObs) { state.resizeObs.disconnect(); state.resizeObs = null; }
  if (state.routeLayer) { state.routeLayer.remove(); state.routeLayer = null; }
  if (state.labelLayer) { state.labelLayer.remove(); state.labelLayer = null; }
  if (state.searchMarker) { state.searchMarker.remove(); state.searchMarker = null; }
  if (state.venueMarker) { state.venueMarker.remove(); state.venueMarker = null; }
  state.customRoute = null;
  if (state.map) { state.map.remove(); state.map = null; }
  state.inputs = [];
  state.onInput = null;
  state.built = false;
  state.leafletAttempt += 1;
  state.leafletPromise = null;
  state.leafletCbs = [];
  state.initialised = false;
}

export default { init, destroy };
