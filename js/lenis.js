/**
 * Lenis smooth-scroll module (placeholder shell).
 *
 * Owner: Agent 09 (Phase 4c).
 *
 * Constitution §2: Lenis allowed on desktop only (`smoothTouch: false`).
 * Will be disabled when prefers-reduced-motion is set, and bypassed on
 * touch devices.
 */
export function init() {
  console.log("lenis: TODO (Agent 09)");
}

export function destroy() {}
